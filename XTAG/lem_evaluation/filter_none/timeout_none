#!/bin/bash

$* &
sleep 1
pid=`ps -C parser.bin -o pid=`

if [[ $pid != "" ]]
then
  i=0
  while ps $pid >/dev/null
  do
    sleep 1
    ((i++))
    if [ $i -ge 1800 ]
    then
      kill $pid
      break
    fi
  done
fi
